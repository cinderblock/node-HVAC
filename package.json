{
  "name": "node-server-ui-base",
  "version": "0.0.0",
  "description": "Coupled daemon and UI with bundled deploy scripts",
  "scripts": {
    "dev": "yarn && yarn daemon && yarn deploy && yarn ui && install-vscode-extensions",
    "ui": "cd ui && yarn",
    "daemon": "cd daemon && yarn",
    "deploy": "cd deploy && yarn",
    "remote": "cd deploy && yarn remote"
  },
  "repository": "git@github.com:cinderblock/node-server-ui-base.git",
  "author": "Cameron Tacklind <cameron@tacklind.com>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "eslint": "^5.14.1",
    "install-vscode-extensions": "^1.0.0",
    "prettier": "^1.16.4"
  },
  "dependencies": {
    "yarn": "^1.13.0"
  }
}
